# Data Explorer Dashboard

A dataset-agnostic desktop data-exploration tool (CSV / Excel / Parquet).
- Load a dataset
- Auto-detect numeric/categorical columns
- Plot histogram / boxplot / scatter / correlation heatmap
- Export filtered subset
- Optional simple linear regression

## Install

```bash
pip install -r requirements.txt
